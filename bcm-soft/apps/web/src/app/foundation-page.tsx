import type { ReactNode } from "react";

export function FoundationPage(): ReactNode {
  return (
    <section aria-labelledby="foundation-title">
      <h1 id="foundation-title">Base web preparada</h1>
      <p>La aplicación está lista para incorporar sus primeras capacidades.</p>
    </section>
  );
}

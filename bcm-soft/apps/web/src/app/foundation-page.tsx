import type { ReactNode } from "react";

export function FoundationPage(): ReactNode {
  return (
    <section aria-labelledby="foundation-title">
      <h1 id="foundation-title">Sesión activa</h1>
      <p>Ingresaste correctamente a BCM SOFT.</p>
    </section>
  );
}
